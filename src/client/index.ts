// Low-level protocol client. Handles spawning the plushie binary,
// wire framing (MessagePack length-prefix or JSONL), and raw
// message send/receive.
//
// This is the escape hatch for advanced users who want to build
// their own abstractions on top of the wire protocol.

export { }
